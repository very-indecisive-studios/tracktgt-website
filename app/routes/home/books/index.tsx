import {
    Container,
    Title,
} from "@mantine/core";
import React from "react";

export default function Books() {
    return (
        <Container py={16}>
            <Title mb={32} order={1}>Books</Title>
        </Container>
    );
}
